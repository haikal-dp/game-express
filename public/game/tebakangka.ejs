<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Game Tebak Angka</title>
</head>
<body>
    <h1>Selamat Datang, <%= username %></h1>
    <p>Saldo Anda: Rp<%= balance.toLocaleString() %></p>

    <form action="/tebakangka/play" method="POST">
        <label for="guess">Tebak angka (1-10): </label>
        <input type="number" id="guess" name="guess" min="1" max="10" required>
        <button type="submit">Mainkan</button>
    </form>

    <% if (typeof message !== 'undefined') { %>
        <p><%= message %></p>
    <% } %>
</body>
</html>
