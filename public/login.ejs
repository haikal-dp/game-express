<!DOCTYPE html>
<html lang="en">
<head>
    <title>Haikung Game</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://unpkg.com/react/umd/react.development.js"></script>
    <script src="https://unpkg.com/react-dom/umd/react-dom.development.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet"/>
    
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1a1a1a;
            color: #ffffff;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 15px;
            box-sizing: border-box;
        }

        .login-section {
            text-align: center;
            width: 100%;
            max-width: 400px;
            padding: 20px;
            background-color: #333333;
            border-radius: 12px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.5);
        }

        .login-section p {
            margin: 10px 0;
            font-size: 16px;
            color: #ffa500;
        }

        .login-section input {
            width: 100%;
            padding: 12px 40px;
            margin: 10px 0;
            border-radius: 25px;
            border: 1px solid #ffa500;
            background-color: #1a1a1a;
            color: #ffffff;
            font-size: 16px;
            box-sizing: border-box;
        }

        .login-section .input-container {
            position: relative;
            width: 100%;
            margin: 15px 0;
        }

        .login-section .input-container i {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            color: #ffa500;
            font-size: 16px;
        }

        .login-section .input-container i:first-child {
            left: 15px;
        }

        .login-section .input-container .eye-icon {
            right: 15px;
            cursor: pointer;
        }

        .login-section .login-buttons {
            margin: 20px 0;
        }

        .login-section .login-buttons button,
        .login-section .login-buttons a {
            display: block;
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border-radius: 25px;
            background-color: #ffa500;
            color: #000000;
            text-decoration: none;
            font-weight: bold;
            font-size: 16px;
            transition: background-color 0.3s;
            border: none;
            cursor: pointer;
        }

        .login-section .login-buttons a:hover,
        .login-section .login-buttons button:hover {
            background-color: #cc8400;
        }

        .login-section .links {
            display: flex;
            justify-content: space-between;
            width: 100%;
            margin: 15px 0;
        }

        .login-section .links a {
            color: #ffa500;
            text-decoration: underline;
            font-size: 14px;
        }

        @media (max-width: 480px) {
            .login-section {
                padding: 15px;
            }

            .login-section input {
                padding: 10px 35px;
            }

            .login-section .input-container i:first-child {
                left: 10px;
            }

            .login-section .input-container .eye-icon {
                right: 10px;
            }

            .login-section .login-buttons button,
            .login-section .login-buttons a {
                padding: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="login-section">
        <p>SIMPAN LINK DI BAWAH INI DI HP ANDA</p>
        <p><span class="text-blue-500">LINK ANTI BLOKIR HaikungGame :</span>haikung.my.id</p>
        <p>Silahkan login untuk mulai bermain</p>

        <form action="/login" method="POST">
            <div class="input-container">
                <i class="fas fa-user"></i>
                <input type="text" name="username" placeholder="Username" required/>
            </div>
            <div class="input-container">
                <i class="fas fa-lock"></i>
                <input type="password" name="password" placeholder="Password" required/>
                <i class="fas fa-eye eye-icon"></i>
            </div>

            <div class="links">
                <a href="#">Lite Mode</a>
                <a href="#">Lupa password?</a>
            </div>

            <div class="login-buttons">
                <button type="submit">LOG IN</button>
                <a href="/daftar">DAFTAR</a>
            </div>
        </form>

        <% if (typeof error !== 'undefined') { %>
            <p style="color: red;"><%= error %></p>
        <% } %>
    </div>
</body>
</html>
